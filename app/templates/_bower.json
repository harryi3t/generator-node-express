{
  "name": "<%= _.slugify(projectName) %>",
  "version": "0.0.0",
  "dependencies": {
    <% if (useAngular) { %>
    "angular": "~1.0.8",<% } %>
    <% if (useJQuery) { %>
    "jquery": "2.0.3",<% } %>
    "modernizr": "~2.6.2"
  },
  "devDependencies": {}
}